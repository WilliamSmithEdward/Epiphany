[toolchain]
# Pinned for reproducible builds (determinism mandate, ROADMAP section 1).
# "stable" resolves to the installed stable toolchain; CI installs the same.
channel = "stable"
components = ["rustfmt", "clippy"]
