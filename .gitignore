# ---- Rust ----
/target/
**/*.rs.bk
*.pdb
# Note: Cargo.lock IS committed (workspace ships binaries). Do not ignore it.

# ---- Node / web ----
node_modules/
dist/
.vite/
*.tsbuildinfo
.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# Note: package-lock.json IS committed for reproducible installs. Do not ignore it.

# ---- Local config & secrets (never commit real secrets, RG-11) ----
.env
.env.*
!.env.example

# ---- Coverage / test output ----
coverage/
*.profraw

# ---- Editor / IDE ----
.idea/
.vscode/*
!.vscode/extensions.json
.claude/
*.swp
*~

# ---- OS ----
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# local dev backend data (verification only, not tracked)
.dev-data/
